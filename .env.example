# Backend configuration
# Kopieer deze file naar .env.local (lokaal) of stel de variabelen in via je hostingomgeving.
OFFR3D_DB_HOST=localhost
OFFR3D_DB_NAME=offr3d
OFFR3D_DB_USER=offr3d_user
OFFR3D_DB_PASS=change-me

# API key gebruikt door PHP endpoints en de frontend (Authorization + CSRF)
OFFR3D_API_KEY=replace-with-secure-key

# Frontend (Vite) gebruikt deze key om fetches te ondertekenen
VITE_API_KEY=
